<%= render 'formTwo' %>

<% if flash[:notice] %>
<div class="notice round new-notice" id="notice"><%= flash[:notice] %></div>
<% end %>

<div id="preloader" style="display:none;" class="upload">
<%= link_to uploadLogo, root_path %>
</div>


<script type="text/javascript">

    var notice = $('#notice');
    if ( notice.text().length > 0 ) { notice.delay(1500).fadeOut('slow'); }

function getCoef(e, url){
	var fullurl = 'http://s2c.microaqua.eu' + url;
	//$.get(fullurl, 
	//	function(data){
	//		//solo cosi
	//	},
	//	dataType='text/csv');
	csvData = $.ajax({
                    type: "GET",
                    url: fullurl,
                    dataType: "text/csv",
                    success: function (result) {
                       alert(result);
                     }
                });
	//e.preventDefault();
	return false;
}
</script>

